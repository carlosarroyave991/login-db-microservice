#version: '3.8'
#services:
#  db:
#    image: postgres:15
#    container_name: login_postgres
#    environment:
#      - POSTGRES_DB=login_db      # <--- AQUÍ se crea la base de datos automáticamente
#      - POSTGRES_USER=postgres
#      - POSTGRES_PASSWORD=root
#    ports:
#      - "5432:5432"
#    volumes:
#      - ./init-db:/docker-entrypoint-initdb.d # Opcional: para el schema